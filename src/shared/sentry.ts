import * as Sentry from '@sentry/node';
import { config } from '../config/env';

// export const initializeSentry = () => {
//   Sentry.init({
//     dsn: config.sentry.dsn,
//     environment: config.sentry.env || 'development',
//     integrations: []
//   });
// };

